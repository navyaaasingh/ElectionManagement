# Arduino Fingerprint Board Connection
Status: In Progress

## Step 1: Verify USB Connection ✅
- `/dev/cu.usbserial-0001` detected
- CP210x drivers OK

## Step 2: Create user_voting_kiosk.ino ✅
- Your code ready for test

## Step 3: Hardware Check
- Ensure fingerprint sensor wired to ESP32 UART2 (GPIO16 RX, GPIO17 TX, GND)
- Buttons on GPIO13,12,14,27

## Step 4: Flash to ESP32
- Install PlatformIO extension in VSCode
- Open iot-terminal/ in VSCode
- PlatformIO: Build → Upload → Monitor

## Step 5: Test
- Connect to WiFi AP 'Voting_Station' password 'password123'
- View dashboard at 192.168.4.1
- Press buttons + fingerprint

## PRODUCTION IOT RUNBOOK

**Firmware**: main.cpp (MQTT backend integration)

**PHASE 1** Firmware Choice: main.cpp ✅

**PHASE 1** Topic Fix: MQTT_TOPIC_VOTE = "election/vote/submit" ✅

**Pass/Fail Criteria:**

1. **Boot & Sensor**
   Expected: "Found fingerprint sensor!" 
   FAIL: "Did not find"
   
2. **WiFi/MQTT**
   Expected: "MQTT connected!" 
   FAIL: "MQTT connection failed"

3. **Biometric Capture**
   Expected: "SHA-256 Hash: [64hex]"
   FAIL: "Failed to capture"

4. **Vote Submit** 
   Expected: "Vote submitted successfully!" → backend iotService.handleVoteSubmission
   
5. **Offline Replay**
   Network down/up → cached votes sync
   
6. **Tamper**
   Trigger GPIO34 HIGH → "TAMPER DETECTED" lock/wipe

**Rollback**: pio run erase + /reset dashboard


 


## Flash & Test Commands:
```
cd iot-terminal
platformio lib install
platformio run --target upload -e esp32dev
platformio device monitor
```

**Expected Logs:**
```
Found fingerprint sensor!  ← YOUR BOARD WORKS
AP IP: 192.168.4.1
```

Connect WiFi `Voting_Station` → `192.168.4.1` dashboard.

Task: Bioboard connected + test code ready!



